package com.shinhan.day04;

//class : object를 만들기 위한 틀(template), 설계도
//object : class를 이용해서 만든 독립적 실체(instance)
public class Book {

	//1.field(변수,data 저장목적, 특징)
	String title;
	int price;
	//2.constructor(생성자메서드) : 기본생성자는 컴파일 시에 만들어진다.
	//return 사용불가
	//생성자를 추가가능
	Book() {
		System.out.println("default 생성자를 재정의함");
	}
	//3.method(함수, 기능)
	//4.block
	//5.inner class
}
